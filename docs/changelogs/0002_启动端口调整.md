# 变更日志: 启动端口调整
**日期**: 2025-12-11
**任务**: 将开发服务器端口改为 3001，确认后端接口仍指向 3000

## 📂 文件变更
### 修改 (Modified) / 新增 (New)
- `package.json`: 将 `dev` 脚本调整为 `next dev -p 3001`，固定开发端口。
- `src/app/login/page.tsx`: 保持登录 API 地址 `http://localhost:3000/auth/login` 不变（仅复核，无改动）。

## 💡 技术说明 (Technical Notes)
- **关键点**: 前端开发端口改为 3001，不影响后端接口地址；如代理/反向代理存在，请同步配置。
- **配置变更**: 无 `.env` 改动；API 仍调用 `http://localhost:3000`。

