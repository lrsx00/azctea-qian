# 变更日志: 登录请求与 CORS 调整
**日期**: 2025-12-11
**任务**: 修正登录请求的跨域设置与令牌存储规范

## 📂 文件变更
### 修改 (Modified) / 新增 (New)
- `src/app/login/page.tsx`: 登录请求显式设置 `mode: "cors"`，兼容后端跨域；使用后端返回的 `access_token`/`token`，存储键统一为 `token`，成功提示使用 shadcn toast 并跳转 `/dashboard`。

## 💡 技术说明 (Technical Notes)
- **关键点**: `fetch` 启用 CORS；本地存储键改为 `token`；保持 API 地址 `http://localhost:3000/auth/login`。
- **配置变更**: 无 `.env` 变动。

